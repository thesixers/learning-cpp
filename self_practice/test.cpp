#include <iostream>
#include <sstream>

using namespace std;

int  main()
{
    // string sentence = "i am a boy that is good and am fine";
    // int arr[4];
    // int length = sentence.length();
    // int count = 0;
    // string Sentence;
    // // stringstream ss(sentence);
    // for(int i = 0; i < length; i++){
    //     if(sentence[i] == ' ') count++;
    //     if(count == 2){
    //         Sentence.insert(Sentence.end(), '\n');
    //         count = 0;
    //     }
    //     Sentence.insert(Sentence.end(), sentence[i]);
    // }
    // cout << Sentence << endl;
    string name;
    cout << "the name is: " << name << endl;
    return 0;
}